# computer-use-plus 开发计划

## 1. 项目定位

`computer-use-plus` 是一个面向 Windows 的 MCP 服务。它的目标不是让 AI 反复观看屏幕并计算坐标，而是将本地桌面转化为可查询、可执行、可验证、可自动复用的状态系统。

AI 只发送简短的操作意图；服务在本地完成元素发现、策略选择、输入注入、状态验证和 UI 记忆更新。核心收益是减少模型可见数据、减少 MCP 往返次数，并降低端到端延迟。

## 2. 设计原则

1. 优先本地结构化数据。能从 Windows UI Automation（UIA）读取的内容，不使用截图或 OCR。
2. 默认由服务端完成定位。模型不需要获得整屏元素或坐标，除非本地无法高置信地完成任务。
3. 默认返回状态增量。成功结果只返回必要的状态变化；图片、完整 OCR 和候选列表均按需降级返回。
4. 所有输入均需验证。点击、输入或快捷键完成后，必须确认页面、控件、窗口或文本出现预期变化。
5. 底层 UI 定位记忆和状态转换由程序自动维护，AI 不能直接改写；主 AI可以显式保存、命名和整理高层 shortcut。
6. 缓存只保存语义信息。绝对坐标仅作弱特征和最后兜底，不能作为长期路径的唯一依据。
7. 按风险扩展感知范围。优先定位当前目标；失败后才扩大到元素列表、局部 OCR、局部视觉或截图。

## 3. 用户可见 MCP 工具

初版只暴露三个工具，避免模型拼装大量底层调用。

### `computer.state`

返回窗口清单、焦点窗口、活动模态窗、运行能力和最近操作的简短状态。

```json
{
  "windows": [["w1", "记事本", "未命名 - 记事本"]],
  "focused": "w1",
  "modal": null
}
```

### `computer.inspect`

按请求返回最小必要观察数据。支持窗口、指定角色、关键字、区域或截图，不默认返回完整 UI 树。

```json
{"window":"w1","query":{"text":"保存","role":"button"}}
```

### `computer.act`

接收一组有序声明式动作，将每个动作作为“定位、执行、验证”的本地事务处理。

```json
{
  "window": "w1",
  "actions": [
    {"click": {"text": "保存", "role": "button"}},
    {"setValue": {"label": "用户名", "value": "alice"}},
    {"hotkey": ["CTRL", "S"]},
    {"keys": ["w", "a", "a", "s"]},
    {"wait": {"state": "saved", "seconds": 2}}
  ]
}
```

连续、带时间的精确按键时间轴使用紧凑格式；这里的 `at` 是唯一保留毫秒的对外时间字段：

```json
{"keys": [{"key": "w", "at": 0}, {"key": "a", "at": 1000}]}
```

成功时仅返回：

```json
{"ok":true,"state":"saved","strategy":"cached-uia","changed":["已保存"]}
```

只有无法本地消歧时才返回紧凑候选或请求更高成本的观察。

## 4. 本地动作事务

每个动作都遵循以下闭环：

```text
标准化动作
-> 解析目标窗口与当前页面状态
-> 用最低成本策略定位目标
-> 激活窗口并验证焦点
-> 执行输入或 UIA 操作
-> 等待事件或状态变化
-> 验证结果
-> 返回紧凑结果并自动更新记忆
```

应优先使用 UIA 的 Invoke、Value、Selection 等模式；无法使用时才注入鼠标键盘。对于输入字段，`setValue` 优先走 UIA ValuePattern，`type` 才模拟真实键盘输入。

## 5. 感知与策略路由

每次定位时，路由器根据可靠性、预计延迟、模型可见字节数和操作风险选择策略：

```text
评分 = 成功概率 / (预计延迟 + 返回数据成本 + 风险惩罚)
```

通用感知严格按四级链路降级：

1. **状态记忆预测**：先用窗口作用域、页面指纹和已验证的状态转换边预测当前/下一界面，复用稳定定位器或完整 shortcut；高置信命中时不重新读取整棵 UI 树，但动作后仍必须验证结果。
2. **本地结构化接口**：优先读取 Windows UIA、Win32 窗口布局和无障碍节点；浏览器等已有稳定原生接口的应用在这一层使用 CDP/Accessibility Tree，而不是强行走桌面截图。
3. **本地坐标感知**：结构化接口缺失或目标未命中时，对目标窗口或局部区域执行 OCR，返回文本、置信度和窗口相对边界框；必要时使用轻量本地视觉识别处理图标、画布和非文本控件。
4. **大模型结构化视觉**：前三层无法可靠消歧时，才发送受限的局部截图，让视觉模型只返回经过 schema 验证的窗口、节点、层级、关系、边界框和置信度，不直接自由生成操作。

绝对坐标只用于明确请求或所有语义策略均不可用的最后兜底，不进入稳定长期路径。

记忆预测必须满足以下条件：窗口类型和作用域一致、来源页面指纹相符、转换边达到最低成功次数和成功率、记录未过期、DPI/主题/应用版本差异未超过阈值。预测输出必须标记 `source=memory` 和置信度；命中失败时立即降级到第二级，并降低该定位器或转换边的可靠性。记忆只减少重复观察，不能跳过动作后验证，也不能把未观察到的节点伪装成当前事实。

大模型视觉返回结构统一为受限数据，不接受说明性文本：

```json
{"windows":[{"id":"w1","nodes":[{"id":"n1","role":"button","text":"完成","bounds":{"x":0,"y":0,"width":1,"height":1},"parent":null,"confidence":0.96,"source":"vision"}]}]}
```

视觉层默认只使用窗口局部截图并限制像素、字节数、节点数和响应 schema；截图不写入长期记忆，成功后立即删除。

路由器必须记录：候选策略、选择策略、置信度、耗时、降级原因、验证结果和失败类别。记录用于优化策略画像，不能直接作为长期 UI 路径。

## 6. 语义定位器

定位器不能只保存文本。一个稳定按钮应由以下特征组合描述：

```text
role=button
name=保存
automationId=saveButton（如存在）
parent=设置页底部操作栏
near=取消
relativePosition=右侧
```

OCR 定位器额外保存相对窗口比例、邻近文本和视觉特征。这样可以处理同名文本、DPI 缩放、窗口移动和轻微布局变化。

当候选无法自动消歧时，服务返回极小的候选集，不返回完整页面：

```json
{
  "need": "disambiguation",
  "candidates": [
    ["保存", "button", "右下角"],
    ["保存草稿", "button", "顶部"]
  ]
}
```

## 7. 自动 UI 学习与缓存

底层 UI 记忆是服务端的状态转换图，不对 AI 暴露写入接口。高层 shortcut 是独立的数据层，向主 AI开放受验证的 `save/rename/merge/archive/restore/organize` 接口。

每次 `computer.act`：

1. 采集操作前页面指纹：窗口元数据、稳定 UIA 元素、OCR 关键文本和布局特征。
2. 执行动作，并通过 UIA 事件、窗口状态、OCR 或小范围视觉差异验证结果。
3. 成功且已验证时，记录“来源状态 -> 标准化动作 -> 结果状态”的转换边，并提升定位器置信度。
4. 目标不存在、无界面变化、发生误点或指纹变化时，降低对应记录置信度并启动重新发现。

每条转换边至少保存来源指纹、标准化动作、语义定位器、结果指纹、成功率、最近使用时间、使用次数和运行环境（应用版本、DPI、窗口类名、尺寸）。

缓存分三级：

- 热缓存：活跃窗口的完整元素快照和最近转换。
- 温缓存：高置信语义定位器和常见状态转换。
- 冷缓存：紧凑页面指纹和统计信息。

淘汰采用“成功率 + 使用次数 + 最近使用 - 失败 - 环境变化 - 时间衰减”的有界评分。重复验证失败的定位器立即禁用；历史转换保留为弱候选，直至低于保留阈值。

## 8. 多窗口与桌面调度

窗口是一等对象，包含稳定 ID、标题、进程、窗口类名、边界、DPI、焦点、层级、模态关系和页面指纹。每个动作必须指定目标窗口，或由服务在仅有唯一可靠候选时解析。

执行前后必须处理：

- 目标窗口激活与焦点验证。
- 多显示器与 DPI 坐标换算。
- 新出现的模态窗、系统对话框和权限窗。
- 被遮挡窗口和前台窗口变化。
- 跨窗口状态转换，例如浏览器下载后打开文件资源管理器。

截图保留原生像素边界，元素以边界矩形返回。默认不叠加永久坐标网格，避免干扰视觉识别。

### 专用执行桌面

Windows 默认使用专用执行桌面或独立执行会话，不应在用户当前桌面抢焦点。轻量后端通过 Win32 `CreateDesktop` 创建命名桌面，并在其中启动独立 agent；主 MCP 服务通过命名管道控制 agent，禁止调用 `SwitchDesktop`。对 GPU、受保护内容或跨桌面 UIA 兼容性不足的应用，应路由至独立用户会话或虚拟机后端。

当前实现默认采用该轻量后端：agent 持有带 `KILL_ON_JOB_CLOSE` 的 Job Object，应用进程树随 agent 一并回收；键盘使用目标 HWND 消息，点击使用 UIA 或窗口消息，不调用全局 `SendInput`。按源码哈希缓存的 agent 可执行文件约 10--30 KB，桌面本身不产生持续磁盘占用。

## 9. 应用能力画像

首次处理应用时，服务自动记录：UIA 覆盖率、OCR 可读性、视觉复杂度、是否有专用接口、缓存稳定性及常见失败类型。

画像用于调整该应用的策略。例如：浏览器优先 CDP，原生设置页优先 UIA，画布软件优先视觉和坐标，UIA 长期不完整的 Electron 应用优先 OCR。

## 10. 实施阶段

### 阶段 1：MVP 基础闭环

范围：Windows 窗口枚举、焦点管理、UIA 读取、语义按钮点击、键鼠输入、动作后验证、短期内存缓存、结构化日志。

不包含：OCR、截图、视觉识别、跨应用适配器、长期持久化记忆。

目标：验证在 UIA 支持良好的应用中，AI 是否能够不看截图完成常用操作。

### 阶段 2：本地文本感知

范围：OCR 抽象、本地 OCR 引擎、文本边界框、OCR 与 UIA 的策略路由、局部区域观察与紧凑候选消歧。

目标：让不支持或部分支持 UIA 的常规桌面应用也能通过语义文本操作。

### 阶段 3：预测式自动记忆

范围：页面指纹、状态转换图、持久化存储、三级缓存、置信度评分、遗忘与失效机制；增加由已验证转换边生成的预测快照、预测节点标记、命中验证和失败降权。

目标：高置信重复流程直接复用完整 shortcut 或预测 UI 状态，在一次 `computer.act` 调用中完成，且无需模型逐步重新观察屏幕。

### 阶段 4：浏览器适配与多窗口

范围：优先解决 Chromium 在 Windows 专用 desktop 中无法枚举窗口的问题；实现独立 profile、CDP/Accessibility Tree、DOM/坐标映射、页面截图、批量动作和 Job Object 回收；同时完善多窗口调度、模态处理和跨窗口转换。

目标：浏览器即使无法暴露稳定 UIA 顶级窗口，也能通过专用 CDP 后端在不影响用户前台的环境中完成公开页面操作；可靠处理浏览器、对话框和文件资源管理器组合流程。

### 阶段 5：结构化视觉与路由加固

范围：实现可插拔视觉 provider，将局部截图转换为严格 schema 的布局树；接入四级策略路由、文件资源管理器及高价值应用适配器；完善权限模型、诊断回放、性能遥测、故障恢复与压力测试。

目标：只有记忆、本地结构化接口和 OCR 都无法可靠处理时才调用视觉模型；在高频应用中优先使用本地路径，进一步降低 token、延迟与失败率。

## 11. 推荐技术决策

- MCP 协议层和 Windows 原生执行层优先使用 C#/.NET 8：UI Automation、Win32 窗口管理、DPI、事件监听和 `SendInput` 的兼容性更好。
- OCR 和视觉应作为可替换的本地接口；推理模型常驻，避免每次动作启动 Python 进程。
- 内部消息和日志使用结构化数据；对 AI 的 MCP 返回结果执行严格的字段和长度控制。
- 优先使用事件驱动验证，避免以截图或 OCR 的轮询作为默认等待方式。

## 12. 验收指标

- 在 UIA 覆盖良好的应用中，常见流程无需截图即可完成。
- 高置信重复流程只需一次 `computer.act`，没有额外的 `inspect` 往返。
- 每个应用分别统计：端到端延迟、模型可见响应字节数、缓存命中率、策略降级率、定位误命中率和已验证任务成功率。
- 所有复用的缓存定位器均在输入注入前完成当前页面指纹验证。
- 操作失败时，返回可恢复的失败原因，而不是误报成功。

## 13. 成本与存储预算

### 13.1 存储分层

默认不把完整截图写入长期记忆。截图只作为短期诊断材料，成功后立即删除，失败截图按 TTL 自动清理。

| 项目 | 默认位置 | 单条估算 | 说明 |
|---|---|---:|---|
| 热缓存 | 内存 | 0.1--10 MB/活跃窗口 | 完整 UIA 元素和最近事件，不要求持久化 |
| 页面指纹 | 磁盘 | 1--5 KB/页面 | 窗口元数据、稳定文本和布局摘要 |
| 语义定位器 | 磁盘 | 0.5--3 KB/元素 | 不保存完整截图，坐标仅作弱特征 |
| 状态转换边 | 磁盘 | 0.5--2 KB/边 | 包含动作、成功率、结果指纹和运行环境 |
| 运行日志 | 磁盘 | 5--50 MB/月 | 默认轮转并限制保留周期 |
| 临时截图 | 磁盘 | 100 KB--2 MB/张 | 仅失败诊断或显式请求，默认短 TTL |
| OCR 模型 | 磁盘 | 100--800 MB | 取决于本地 OCR 引擎和语言包 |
| 视觉模型 | 磁盘 | 100 MB--1 GB+ | 初版可选，不应成为基础安装的必需项 |

典型个人使用量（10 个应用、每个 100 个页面、每个页面 10 条常用转换）预计为：

- 不含模型：约 10--50 MB 持久化数据，热缓存占用约 100 MB 以内。
- 含一个 OCR 模型：约 150--850 MB 安装体积。
- 含视觉模型：通常约 300 MB--2 GB，取决于模型大小和量化方式。

高频重度使用（100 个应用、约 10,000 个页面、约 100,000 条转换）预计为：

- 结构化记忆约 100--500 MB。
- 加上日志、版本并行缓存和临时诊断文件，建议预留 1--2 GB。
- 完整截图不应进入长期记忆；如果开启截图留存，必须设置总容量上限、单文件上限和 TTL。

### 13.2 运行成本

- 常驻服务：主要成本是内存和少量 UIA 事件监听，目标是 100--300 MB 基础内存。
- OCR：首次加载模型有初始化成本；模型常驻后，单个局部区域应明显低于一次远程视觉模型调用的延迟。
- 视觉：只在 UIA/OCR 无法可靠定位时触发，避免作为默认路径。
- 网络和模型费用：通过减少截图上传、模型输入 token 和观察往返来节省，具体金额取决于所使用的模型价格。

### 13.3 是否划算的判断

该方案在以下场景通常划算：重复操作多、窗口状态相对稳定、可使用 UIA 或本地 OCR、远程模型调用延迟或 token 价格较高。

收益主要来自：

1. 一次 `computer.act` 事务替代多次观察与点击往返。
2. 成功缓存后不再向模型发送完整 UI 树或截图。
3. 本地 UIA/OCR 取代远程视觉识别。
4. 失败只返回增量诊断，减少无效上下文。

如果只是偶尔操作一次、应用界面高度动态或每次都需要复杂视觉判断，长期记忆的收益会较低；此时应限制缓存规模，只保留短期热缓存。

### 13.4 成本验收指标

除成功率和延迟外，必须测量：

- 每次任务的模型输入/输出 token 与可见响应字节数。
- 每次任务的 MCP 往返次数和截图次数。
- UIA、OCR、视觉、坐标各策略的调用比例。
- 热/温/冷缓存命中率和缓存失效率。
- 每个应用的磁盘占用、内存峰值、OCR/视觉 CPU 时间。
- 单次任务节省的模型成本与本地资源成本的比值。

## 14. 当前实施状态（2026-08-11）

- 已完成 Windows 专用执行桌面 MVP：默认使用 `backgroundOnly` 隔离模式，不调用 `SwitchDesktop`，不向用户当前桌面发送全局鼠标或键盘输入。
- 已完成 UIA 优先、OCR 降级、窗口元数据截图、隔离 HWND 消息输入、`kbseq`/`kbops` 紧凑格式和 Windows Job Object 进程树回收。
- 已完成由程序自动维护的 UI 定位记忆、状态转换采样、容量/TTL/可靠性遗忘；主 AI只能通过受验证的 shortcut 接口写入高层动作链。
- 已实现预测式 UI 快照：重复且成功率达阈值的状态转换边保存受限节点和环境指纹，`computer.state` 可按动作签名复用并标记 `source=memory`/置信度；预测不跳过动作后验证，环境不兼容或失败会立即降级并降权。
- 已完成 action-ready `computer.state.snapshot`、短期节点 `ref` 和最近转换摘要，使模型可一次规划并提交完整动作链，而不必每一步重新读屏。
- 已完成可选快速操作 AI：只规划和执行当前动作，不写长期记忆；与低频整理 AI 共用可选 API key。用户可选择 key 文件配置，也可跳过并继续使用全部本地功能。整理 AI 默认只返回 proposal，只有主 AI 显式传入 `applyAi:true` 或审核后的 `apply` 才应用。
- 已完成主 AI 显式 shortcut 写入和别名管理，以及单窗口稳定类型和跨窗口有序路径的独立存储、运行、重命名、合并、归档和恢复。
- 已完成保守本地聚类：只自动合并参数等价或只差安全等待的重复流程；含额外点击、输入和按键的差异进入候选，不直接物理合并。
- 高层时间参数和 `wait.seconds` 统一使用秒并允许小数，程序内部换算为整数毫秒；仅精确键盘时间轴 `kbops.at` 保留毫秒。
- `computer.state.metrics` 定位为成本与延迟遥测，不承载动作链；链路上下文由 `snapshot + transitions + shortcut` 提供。
- 当前通用识别已完成“记忆预测 -> Windows UIA/Win32 或浏览器 CDP Accessibility Tree -> 带坐标 OCR -> 严格结构化视觉”四级路由；视觉 provider 限制图片字节、节点数、schema 和临时文件 TTL，未配置时保持关闭。
- 已新增独立 profile 的 Chromium CDP 驱动和 `computer.browser` 工具；在专用 desktop 中通过执行 agent 启动，避免复用用户 profile。Edge 在本机专用 desktop 的顶层 UIA 枚举仍可能为空，CDP 是该场景的首选后端。
- 单元与集成测试覆盖隔离桌面、快照 ref、预测命中/失效、快速 AI、shortcut 参数插值和小数秒、严格视觉 schema 与 CDP/ARIA 驱动；当前完整测试为 40 项通过。测试期间仅有 EasyOCR/PyTorch 的 `pin_memory` 警告，不影响功能。
- 当前工作目录 `.data/` 实测约 113 KB（不含 OCR 模型），代理本体按源码哈希缓存，通常为十几 KB，旧日志、过期截图和旧版本代理会按上限清理。浏览器 profile 只在独立实验/运行会话中存在，停止后由用户或 TTL 清理；OCR 模型占用单独计算，取决于本地 EasyOCR 模型与语言包。
- 当前明确支持 Windows 10/11；其他操作系统暂不提供执行后端。

## 15. Native tool-call 与模型适配增量计划（2026-08-11）

### 已实现

- `src/tool-call.js` 统一 `tool_call` 事件，归一化 OpenAI/Anthropic/Gemini/Responses 结果，并支持流式参数拼接。
- `src/providers.js` 支持 chat completions、Responses、Anthropic Messages、Gemini 请求构造。
- `computer.invoke`、`shortcut.run`、`computer.verify`、`computer.cancel` MCP 工具。
- `computer.fast` 优先 native tool call，失败时兼容旧 JSON actions。
- 快速 AI 不具备长期 shortcut 写入权限；成功动作仍由程序自动记录底层记忆。
- 高风险操作返回一次性确认令牌，令牌绑定窗口、动作/shortcut 和参数，120 秒过期且只能使用一次。
- `src/benchmark.js` 与 `npm run benchmark` 统计成功率、P50/P95、token、MCP 往返、截图字节、恢复次数和估算成本。

### 后续实现

- 将 provider 的流式网络响应接入 MCP agent loop，工具调用完成参数校验后立即执行。
- 为本地 action-ID 路由器增加规则/分类模型插拔接口，并用 Edge、Minecraft、微信基准集选择默认模型。
- 将高风险分类扩展为可配置策略，并为跨窗口 shortcut 增加统一确认摘要。
- 完成真实应用端到端 benchmark；默认使用 Edge 独立 profile、Minecraft 独立实例、微信独立配置。

## 16. 稳定性与真实验收阶段（2026-08-13）

### 当前缺口

- Edge、Minecraft、微信尚未形成可重复运行的真实任务清单、环境检查和统一报告；现有 benchmark 只能汇总已有样本。
- `computer.verify` 目前只比较 UI 指纹，无法直接断言文本、控件状态、浏览器 URL 或本地文件结果。
- 高风险操作仍依赖固定关键词，缺少应用级规则、可配置策略和整条 shortcut 的风险摘要。
- provider 虽能读取流式 tool call，但快速执行链仍以完整调用到达后执行为主；本地自然语言路由仍是名称/别名匹配。
- OpenAI、Anthropic、Gemini 和本地视觉/路由模型尚未进行真实 API 延迟、成功率、token 与费用对比。

### 本阶段交付顺序

1. 建立声明式 benchmark suite：支持环境前置条件、重复次数、MCP 工具步骤、预期断言、资源基线与 JSON 报告；内置 Edge、Minecraft、微信模板，但默认不启动应用或执行高风险动作。
2. 扩展 `computer.verify`：支持窗口指纹、文本/角色/automationId 的存在或消失、控件 enabled/value、CDP URL、窗口标题以及限定目录中的文件存在/大小断言。
3. 新增可配置风险策略：默认仅高风险确认；支持自定义规则、应用/进程作用域、allow/confirm/deny 决策、整链摘要和单次确认 token。
4. 为上述能力增加 mock 和协议测试，保持现有记忆、OCR、CDP 与专用桌面测试全部通过。
5. 在真实应用可用且用户已建立独立实例/配置后，分别运行 Edge、Minecraft、微信 suite，记录失败点并据此迭代驱动层。

### 验收目标

- benchmark suite 在 dry-run 下可完整验证配置，不产生桌面副作用；实跑结果包含任务成功率、P50/P95、策略、截图、token、恢复和失败断言。
- `computer.verify` 的每项断言均返回 expected、actual、passed；任意必需断言失败时整体 `ok=false`。
- 风险策略默认拒绝显式 deny，要求 confirm 的动作必须持有匹配整链摘要的一次性 token；普通导航不增加确认往返。
- 完整自动化测试保持零失败；真实应用性能目标仍为 shortcut P95 <100ms、UIA/CDP P95 <300ms、本地 OCR P95 <800ms。
## 17. DeepSeek Harness 适配与发布（2026-08-13）

### 已完成

- 已以官方 `@deepseek-ai/dsh-mcp-client` 作为首选集成方式：Harness 通过 stdio 启动现有 MCP 服务，不复制电脑操作运行时。
- 已新增 `adapters/deepseek-harness/cordis.yml`，支持 `COMPUTER_USE_PLUS_ROOT` 覆盖项目位置，并启用专用 `harness` 工具档。
- `harness` 工具档只公开 `shortcut_run`、`computer_invoke`、`computer_state`、`computer_inspect`、`computer_verify`、`computer_cancel`；名称不含点号，避免 Harness 的工具名规范化哈希和额外 token。
- 已新增适配器单元测试，并完成普通 MCP 与 Harness profile 的 stdio 初始化/工具发现烟测。
- 发布前 `.gitignore` 已补充运行数据、浏览器 profile、密钥文件、临时媒体和部分下载文件的排除规则。

### 待完成

1. 在目标环境安装 DeepSeek Harness 后，用实际 Harness host 做一次无需 API key 的连接/工具发现验收。
2. 使用真实模型运行 Edge、Minecraft、微信 benchmark，比较 shortcut/UIA/CDP/OCR/视觉路径的成功率、P50/P95、token 与成本，再选定默认模型组合。
3. 根据真实第三方自绘界面表现，补充微信、QQ 等应用的隔离实例启动参数和专用验证用例。
4. 将项目初始化为 Git 仓库并发布到用户指定的 GitHub/GitLab 仓库；严禁提交 `.data`、密钥、浏览器 profile 或媒体文件。
